# Cloudflare Pages / Workers – déploiement du site statique (dist/)
# Utilisé quand le build est suivi de : npx wrangler deploy

name = "attitude-voyages"
compatibility_date = "2026-02-19"

[assets]
directory = "./dist"
# SPA : les routes (ex. /contact) renvoient index.html
not_found_handling = "single-page-application"
