# GitHub Pages : ce fichier n’est pas utilisé.
# Fallback SPA = copie de index.html vers 404.html dans le workflow (.github/workflows/deploy.yml).
# Redirections 301 (http → https, www → non-www) : à configurer via DNS ou Cloudflare (voir README).
